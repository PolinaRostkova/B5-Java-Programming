package day27_wrapper_arrayList;

public class MaxAndMinValues {
    public static void main(String[] args) {

        // in each wrapper class we have some useful (already pre-built) reusable methods, values we can use

        /*
            Character.isUpperCase(char);
            Character.isLowerCase(char);
            Character.isDigitCase(char);
         */

        // the range the byte can hold
        System.out.println(Byte.MAX_VALUE);
        System.out.println(Byte.MIN_VALUE);

        // Finding the range that int can hold
        System.out.println(Integer.MAX_VALUE);
        System.out.println(Integer.MIN_VALUE);

        // double range can hold
        System.out.println(Double.MAX_VALUE);
        System.out.println(Double.MIN_VALUE);
    }
}
